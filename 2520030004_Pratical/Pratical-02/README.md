OS Commands and Their Uses:

strace - Shows the system calls made by a Linux command or program.

open() - Opens or creates a file.

read() - Reads data from a file.

write() - Writes data into a file.

close() - Closes a file after using it.
